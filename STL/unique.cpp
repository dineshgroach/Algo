// it is used to remove duplicate set of values from the range and gives the iterator to the new starting of the container
int m = unique(s.begin(), s.end()) - s.begin();

// it convert "000111011" into "0101" and m is the size of the new string
