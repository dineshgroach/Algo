// custom function to get ceil of two numbers given in integer format works on the below principal
// ceil(a/b) can be written as (a+b-1)/b in normal way

int getceil(int64_t a,int64_t b)
{
    return (a+b-1)/b;
}
